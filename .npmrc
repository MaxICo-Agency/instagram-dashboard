# Disable pnpm's minimumReleaseAge supply-chain gate for CI/Docker builds.
# Our lockfile is trusted; without this, freshly-published transitive deps
# (e.g. electron-to-chromium) block `pnpm install --frozen-lockfile`.
minimumReleaseAge=0
minimum-release-age=0
